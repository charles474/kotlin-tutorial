fun main() {
    var number:  Int = 10
    var name: String = "Kotlin"

    /*
        Any data type is the parent/ root type all types come from.
        A variable declared as any can hold any data type.

        Ideally, avoid using this data type to avoid confusion.
     */
    var any: Any = 10
}